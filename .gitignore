# Dependencies
node_modules

# Build output
dist
dist-ssr
*.local
src-tauri/target

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Env / secrets (kept local)
.env
.env.*
!.env.example
# ...except the demo flag, which carries no secrets and must ship for `npm run dev:demo`
!.env.demo

# Local-only AI / build context (never publish)
CLAUDE.md
HANDOFF.md
REDESIGN_PLAN.md
.claude/
.specstory/
.uiaudit/
__verify/

# Local example data for `npm run dev:demo` — real books never get committed
src/dev/seed.local.ts

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Media / recordings / transcripts (building context — never publish)
*.mp4
*.mov
*.mkv
*.webm
*.avi
*.wav
*.mp3
*.m4a
*.srt
*.vtt
frames/

# Heavy tooling binaries
.ffmpeg/
ffmpeg*
ffprobe*
*.zip
