/**
 * Plain (non-CSS-module) class marking the widget's drag affordance.
 * react-rnd's `dragHandleClassName` matches against this exact token, so it must
 * stay a stable global string (not a hashed module class).
 */
export const DRAG_HANDLE = "cajaDragHandle";
